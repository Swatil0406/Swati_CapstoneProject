apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: swati599/backend-app:5
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: MONGO_CONN_STR
              value: "mongodb://mongo:27017/tasksdb"
            - name: USE_DB_AUTH
              value: "false"
